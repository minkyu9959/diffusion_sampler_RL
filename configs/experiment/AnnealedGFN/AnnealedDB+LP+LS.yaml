# @package _global_

wandb:
  tags: ["AnnealedGFN", "LP", "LS"]

defaults:
  - override /model: CMCD
  - override /train: AnnealedGFNTrainer

train:
  train_mode: both_ways

  exploratory: false

  epochs: 10000
