# @package _global_

wandb:
  group: "AnnealedGFN"

defaults:
  - override /energy: manywell
  - override /model: GFN-PIS
  - override /train: AnnealedGFNTrainer

model:
  t_scale: 1.0

  flow_model: null

  langevin_scaler:
    out_dim: 1

  learned_variance: true

train:
  train_mode: fwd
  bwd_loss: null

  # exploratory: true
  # exploration_factor: 0.2
  # exploration_wd: true

  epochs: 10000
  local_search: null