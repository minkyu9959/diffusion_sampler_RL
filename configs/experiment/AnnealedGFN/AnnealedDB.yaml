# @package _global_

wandb:
  group: "AnnealedGFN"

defaults:
  - override /energy: 9gmm
  - override /model: GFN-PIS
  - override /train: AnnealedGFNTrainer

model:
  t_scale: 5.0
  langevin_scaler: null
  flow_model: null

train:
  epochs: 25000
  local_search: null

