# @package _global_

defaults:
  - override /model: AnnealedGFN
  - override /train: SimulationFree

train:
  epochs: 50000

  batch_size: 60000

  # 600 * 100 * 10
  buffer_size: 120000

  sample_interval: 10
  simulation_sample_size: 600
