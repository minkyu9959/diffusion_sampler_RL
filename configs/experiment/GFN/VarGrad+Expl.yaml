# @package _global_

wandb:
  tags: ["GFN-PIS", "VarGrad", "Exploratory"]

defaults:
  - override /model: GFN-PIS

model:
  t_scale: 1.0

  backward_model: null
  langevin_scaler: null
  flow_model: null

train:
  train_mode: fwd
  fwd_loss: tb-avg

  exploratory: true
  exploration_wd: true
  exploration_factor: 0.2

  local_search: null