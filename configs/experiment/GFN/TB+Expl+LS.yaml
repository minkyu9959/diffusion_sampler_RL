# @package _global_

wandb:
  tags: ["GFN-PIS", "TB", "Exploratory", "LS"]

defaults:
  - override /model: GFN-PIS

model:
  backward_model: null
  langevin_scaler: null
  flow_model: null

train:
  train_mode: both_ways
  fwd_loss: tb

  exploratory: true
  exploration_wd: true
  exploration_factor: 0.1
