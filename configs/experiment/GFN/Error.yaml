# @package _global_

wandb:
  tags: ["Debugging"]

defaults:
  - override /model: GFN-PIS
  - override /train: GFN+OffPolicy

model:
  t_scale: 1.0

  backward_model: null
  langevin_scaler: null
  flow_model: null

train:
  train_mode: both-ways
  fwd_loss: tb-avg
  bwd_loss: tb

  exploratory: true
  exploration_wd: true
  exploration_factor: 0.2

  local_search: null