# @package _global_

defaults:
  - override /model: annealed_gfn
  - override /energy: 9gmm

train:
  both_ways: false
  bwd: false
  # Train from only forward trajectory for fair comparison with original DB.

  exploratory: false
  mode_fwd: annealed-db

  local_search:
    do_local_search: false
