# @package _global_

defaults:
  - override /model: GFN
  - override /train: OnPolicy

model:
  t_scale: 1.0

  backprop_through_state: true

  backward_policy: null
  flow_model: null

train:
  fwd_loss: vae

  exploratory: false