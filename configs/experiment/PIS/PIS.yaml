# @package _global_

wandb:
  tags: ["PIS"]

defaults:
  - override /model: GFN-PIS
  - override /train: GFN+OnPolicy

model:
  t_scale: 1.0

  backward_model: null
  flow_model: null
  langevin_scaler: null

train:
  fwd_loss: pis

  exploratory: false