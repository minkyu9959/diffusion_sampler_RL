# @package _global_

wandb:
  tags: ["PIS", "LP"]

defaults:
  - override /model: GFN-PIS

model:
  t_scale: 1.0

  backward_model: null
  flow_model: null

  # LP per dimension vs. Scalar LP
  # langevin_scaler:
  #   out_dim: 1

train:
  epochs: 10000
  train_mode: fwd
  fwd_loss: pis

  exploratory: false
  local_search: null